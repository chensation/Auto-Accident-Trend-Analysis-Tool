import React from 'react';
import Button from '@material-ui/core/Button';

function Climate() {
  return (
    <div>
      <Button variant="outlined" color="default">This Button is where Climate code goes</Button>
    </div>
  );
}

export default Climate;