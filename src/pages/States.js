import React from 'react';
import Button from '@material-ui/core/Button';

function States() {
  return (
    <div>
      <Button variant="outlined" color="default">This Button is where States code goes</Button>
    </div>
  );
}

export default States;